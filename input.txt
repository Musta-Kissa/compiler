extern fn allocate_page() -> *int {}
extern {
    fn print_int(int i) {}
    fn print_double(float i) {}
}
fn tac_test_2(float a, float b, int c, bool d, *int ptr) -> int {
    while( c > 0  && (b >= 2. || true)) {
    //while( c > 0  && b >= 2. || true) {
        print_int(c);
        print_double(b);
        if( false ) {
            print_int(5556666);
        } else if c == 32 {
            print_int(1177755555);
        } else {
            print_int(1999222);
        }
        c = c - 1;
        b = b / 2.;
    }
    *ptr = c;
    print_int(*ptr);
    print_int(c);
    y := a > b;
    if y {
        a = b + b - a;
        b = a - b;
    }
    d = true;
    i :int = 123420;
    x :int = 1235;
    return i;
}

fn main() {
    tac_test_2(923., 1024., 69, true);
}
EOF

fn tac_test(float a, float b, int c, bool d) -> int {
    a = b + b;
    d = true;
    i :int = 0;
    while a > b && a < b {
        a = a - b;
        i = i + 1;
    }
    if( a > b ) {
        c = 1;
    } else if ( d ) {
        c = 2;
    } else if ( a < b ) {
        c = 3;
    } else {
        c = 4;
    }
    return i;
}

fn foo(int a_1, int b_1, int c_1) -> int {
    a:= a_1;
    b:= 4;
    {
        b:= 2;
    }
    {
        b:= 3;
        c:= b * 2;
    }
    return a + c_1 * c_1 + b_1;
}
fn taking_addresses() {
    a : int = 3;
    b :*int = &a;
    print_int(*b);
    *b = 420;
    print_int(*b);
}
fn modify_by_ptr(*int var, int value) {
    *var = value;
}

fn main() {
    ab: int = 32;
    bc: int = 64;
    if ab > bc {
        print_int(ab);
    } else {
        print_int(bc);
    }

    i :bool = (0 == 0);

    a :int = foo(9,2,3);

    modify_by_ptr(&a, 00);

    var_bool :bool;
    var_bool = (0 == 0);

    while( var_bool ) {
        print_int(a);
        a = a + 1;
        var_bool = a < 20;
    }
    print_int(58008);

    if i {
        print_int(a);
    } else {
        print_int(1234);
    }

    taking_addresses();

    print_int(*allocate_page());

    b : int = 69;
    a = 69/3;
    print_int(a);
}
