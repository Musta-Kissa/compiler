int foo(int a,float b) {
    int f = 1+3*9;
    int f = ++a[b];
    int f = ++a.b ;
    int f = !a.b ;
    int f = (--a).b ;
    int f = b;
}
int bar(int c,float d) {
    int e = foo(c,d) + 3;

}
int global = foo(c,d) + 3;

int bazz(int c,float d) {
    int e = foo(c,d) + 3;
    if ( c ) {
        int inside_if = hello + 3 * 0;
    }
    for( int i = 0; b < c ; c + 1) {
        int inside_for = hello + 3 * 0;
    }
    while( true ) {
        int inside_while = hello + 3 * 0;
    }
}
